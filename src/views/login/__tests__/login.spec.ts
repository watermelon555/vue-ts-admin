import { shallowMount } from '@vue/test-utils'

/*describe('test LangSelect', () => {
    test('选择不同的语言', () => {
        const wrapper = shallowMount(LangSelect)
        expect(wrapper.vm.language).toBe('')
    })
})*/

describe
it('should ', function () {
    
}); 
